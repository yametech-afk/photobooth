/**
 * QA Pack — functions/package.json additions (snippet)
 * ============================================================
 * ID modules: `test:rules` (hinahanap ng devops/.github/workflows/pr-checks.yml)
 * at mga devDeps na kailangan ng security rules tests.
 *
 * Paano gamitin: pagsamahin ang "scripts" at "devDependencies" block sa ibaba
 * papunta sa functions/package.json (huwag palitan ang mga mayroon na).
 *
 * ⚠️ Hindi ko ito na-install sa sandbox — i-run ang `npm install` sa functions/
 * pagkatapos i-merge, tapos `npm run test:rules` para i-verify.
 * ============================================================
 */
{
  "_qa_pack_note": "Merge snippet lang — hindi buong package.json",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore,storage,auth --project photobooth-app-dev -- jest -c test/jest.config.rules.js --runInBand",
    "test:unit": "jest -c test/jest.config.rules.js --testPathIgnorePatterns /test/rules/"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.3",
    "firebase-tools": "^13.0.0"
  }
}
